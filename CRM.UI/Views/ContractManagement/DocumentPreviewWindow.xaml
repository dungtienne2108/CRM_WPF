<Window x:Class="CRM.UI.Views.ContractManagement.DocumentPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM.UI.Views.ContractManagement"
        mc:Ignorable="d"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Document Preview" Height="600" Width="800">
    <Grid>
        <!-- Ảnh -->
        <Image x:Name="PreviewImage"
               Stretch="Uniform"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               Visibility="Collapsed"/>

        <!-- Text -->
        <ScrollViewer x:Name="TextContainer" Visibility="Collapsed">
            <TextBox x:Name="PreviewText"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     BorderThickness="0"
                     Background="Transparent"
                     FontSize="14"/>
        </ScrollViewer>

        <!-- PDF / Office / khác -->
        <wv2:WebView2 x:Name="WebViewer" Visibility="Collapsed"/>
    </Grid>
</Window>
