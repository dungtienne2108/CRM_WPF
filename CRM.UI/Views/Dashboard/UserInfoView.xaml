<Window x:Class="CRM.UI.Views.Dashboard.UserInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRM.UI.Views.Dashboard"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Thông tin người dùng" Height="400" Width="450">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="ValueStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Thông Tin Cá Nhân" 
                   FontSize="24" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="#2C3E50"/>

        <!-- Tên -->
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Tên:" Style="{StaticResource LabelStyle}" Width="120"/>
            <TextBlock x:Name="txtName" Text="{Binding Name}" Style="{StaticResource ValueStyle}"/>
        </StackPanel>

        <!-- Ngày sinh -->
        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <TextBlock Text="Ngày sinh:" Style="{StaticResource LabelStyle}" Width="120"/>
            <TextBlock x:Name="txtBirthDate" Text="{Binding BirthDate}" Style="{StaticResource ValueStyle}"/>
        </StackPanel>

        <!-- Email -->
        <StackPanel Grid.Row="6" Orientation="Horizontal">
            <TextBlock Text="Email:" Style="{StaticResource LabelStyle}" Width="120"/>
            <TextBlock x:Name="txtEmail" Text="{Binding Email}" Style="{StaticResource ValueStyle}"/>
        </StackPanel>

        <!-- Số điện thoại -->
        <StackPanel Grid.Row="8" Orientation="Horizontal">
            <TextBlock Text="Số điện thoại:" Style="{StaticResource LabelStyle}" Width="120"/>
            <TextBlock x:Name="txtPhone" Text="{Binding PhoneNumber}" Style="{StaticResource ValueStyle}"/>
        </StackPanel>

        <!-- Nút Đăng xuất -->
        <Button Grid.Row="10" 
                x:Name="btnLogout"
                Content="Đăng Xuất" 
                Height="40"
                Width="150"
                FontSize="16"
                Background="#9B59B6"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"
                Command="{Binding LogoutCommand}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#E74C3C"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="5"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#8E44AD"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
