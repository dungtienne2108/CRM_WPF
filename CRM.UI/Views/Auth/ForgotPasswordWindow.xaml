<Window x:Class="CRM.UI.Views.Auth.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:CRM.UI.Views.Auth"
        mc:Ignorable="d"
        Title="Quên mật khẩu" Height="200" Width="400">
    <StackPanel Margin="20">
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 x:Name="EmailTextBox"
                   materialDesign:HintAssist.Hint="Email"
                   Height="30" Margin="0,5"/>
        <Button Content="Gửi yêu cầu" Height="35" 
                Command="{Binding SendResetPasswordEmailCommand}"/>
    </StackPanel>
</Window>
